from setuptools import setup, find_packages

setup(
    name="src",
    version="0.1",
    packages=find_packages(),
    # install_requires=[      # ADD THIS SECTION
    #     "pandas",
    #     "scikit-learn",
    #     "mlflow",
    #     "dvc",
    #     "pyyaml"
    # ],
)